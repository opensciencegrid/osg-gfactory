# osg-gfactory

## File naming convention
`XX-<description>-<factory>.xml`
- The number can be bumped up to override entries for a specific reason (migration, testbed entries..)
- Factories can be CERN, OSG, FNAL, a combination separated with an underscore (e.g. `cern_osg`), or `all`.
